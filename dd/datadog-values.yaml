datadog:
  apiKeyExistingSecret: datadog-secret
  appKeyExistingSecret: datadog-secret
  site: us5.datadoghq.com
  kubelet:
    tlsVerify: false
    
providers:
  aks:
    enabled: true

# kubectl create secret generic datadog-secret --from-literal api-key=<API_KEY> --from-literal app-key=<APP-KEY>
# helm install datadog -f datadog-values.yaml --set targetSystem=<TARGET_SYSTEM> datadog/datadog
